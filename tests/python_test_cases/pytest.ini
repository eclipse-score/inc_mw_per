[pytest]
addopts = -v
testpaths = tests
markers =
        PartiallyVerifies
        FullyVerifies
        Description
        TestType
        DerivationTechnique

; Additional environment variables
env =
    D:RUST_BACKTRACE = 1
    D:MW_LOG_CONFIG_FILE = config/logging.json
